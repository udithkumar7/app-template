# Activate with: --spring.profiles.active=dev

# Server
server.port=8080

# H2 Database (in-memory for dev to avoid file locking)
spring.datasource.url=jdbc:h2:file:./data/testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# PDF Generation
pdf.template.directory=templates/pdf
pdf.output.directory=output

# Email (use a test account or mailtrap for dev)
# Option 1: Gmail (requires app password) - ACTIVE CONFIGURATION
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=udith644@gmail.com
spring.mail.password=rdaxrytifdxotnyl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Option 2: Mailtrap (recommended for development) - COMMENTED OUT
# Sign up at https://mailtrap.io/ and get your SMTP credentials
#spring.mail.host=live.smtp.mailtrap.io
#spring.mail.port=587
#spring.mail.username=your-mailtrap-username
#spring.mail.password=your-mailtrap-password
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true

# Option 3: Console logging for testing (no actual emails sent)
# Uncomment to just log emails to console instead of sending
#spring.mail.host=localhost
#spring.mail.port=1025

# Enable console-only mode for development (emails will be logged instead of sent)
# Set to false to send real emails via Gmail
app.email.console-only=false